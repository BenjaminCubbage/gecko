{
  "name": "bdcanvas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "copy-wasm": "node copy-wasm.js",
    "dev":       "\"npm run copy-wasm\" && vite --host --port 8080",
    "build":     "\"npm run copy-wasm\" && vite build",
    "preview":   "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.7"
  }
}
